node_modules/
.next/
out/
.env
.env.local
*.log
.DS_Store
next-env.d.ts
.tsbuildinfo
*.tsbuildinfo

# Локальні робочі правила — не для публічного репо
CLAUDE.md

# Зібрані запити (flywheel) — це дані рантайму, не код
data/miss-log.jsonl

# Перезібрані сторінки (npm run scrape) — регенеруються, канон у data/raw/*.uk.txt
data/raw/fetched/
.gstack/
